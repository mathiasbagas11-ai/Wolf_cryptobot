"""Anomaly scanner — coiling / volume-anomaly detection for spot swings.

A *paper-mode* module bolted onto the news bot: it screens a filtered universe
of mid-caps for pre-breakout compression (the BANK / LAB / RAVE pattern), scores
each candidate deterministically, and logs the signals for expectancy
evaluation. It never auto-executes — every signal is advisory only.
"""
